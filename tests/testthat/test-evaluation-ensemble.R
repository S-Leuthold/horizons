#' Tests for build_ensemble() Function

library(testthat)
library(horizons)

test_that("build_ensemble validates inputs", {
  skip("Need finalized workflows fixture - save for later")
})

test_that("build_ensemble creates stacked ensemble", {
  skip("Need finalized workflows fixture - save for later")
})

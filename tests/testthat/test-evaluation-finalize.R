#' Tests for finalize_top_workflows() Function

library(testthat)
library(horizons)

test_that("finalize_top_workflows validates inputs", {
  skip("Need evaluation results fixture - save for later")
})

test_that("finalize_top_workflows selects top N models", {
  skip("Need evaluation results fixture - save for later")
})

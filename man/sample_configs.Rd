% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-sample_configs.R
\name{sample_configs}
\alias{sample_configs}
\title{Sample Model Configurations with Optional Factorial Design Support}
\usage{
sample_configs(
  configs,
  n_per_group = 5,
  ensure_baseline = TRUE,
  factorial_pairs = NULL,
  verbose = FALSE
)
}
\arguments{
\item{configs}{Data frame of all possible model configurations}

\item{n_per_group}{Integer. Number of configurations to sample per pipeline block (default: 5)}

\item{ensure_baseline}{Logical. Include at least one no-covariate config per block (default: TRUE)}

\item{factorial_pairs}{List of character vectors. Covariate pairs for factorial coverage (default: NULL)}

\item{verbose}{Logical. Print detailed sampling diagnostics (default: FALSE)}
}
\value{
A data frame of sampled configurations maintaining the structure of the input
}
\description{
Intelligently samples model configurations from the full configuration space.
Ensures baseline coverage (no covariates) and supports factorial designs for
covariate interaction analysis. Uses stratified sampling to maintain balance
across model types, preprocessing methods, and feature selection approaches.
}
\details{
The function operates in four phases:
\enumerate{
\item Baseline reservation: Ensures each pipeline block has a no-covariate configuration
\item Factorial reservation: Creates 2x2 factorial designs for specified covariate pairs
\item Budget calculation: Determines remaining slots per pipeline block
\item Diverse sampling: Fills remaining slots with stratified random sampling
}

A pipeline block is defined as a unique combination of:
model × transformation × preprocessing × feature_selection
}
\examples{
\dontrun{
# Basic usage with defaults
sampled <- sample_configs(all_configs, n_per_group = 5)

# With factorial design for covariate interactions
sampled <- sample_configs(all_configs, 
                         n_per_group = 10,
                         factorial_pairs = list(c("Clay", "pH"), 
                                              c("AI", "MAP")))
}

}

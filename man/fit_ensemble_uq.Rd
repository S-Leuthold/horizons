% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble-uq.R
\name{fit_ensemble_uq}
\alias{fit_ensemble_uq}
\title{Fit CV+ Conformal Uncertainty Quantification for a Stacked Ensemble}
\usage{
fit_ensemble_uq(x, level = DEFAULT_UQ_LEVEL, seed = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A \code{horizons_ensemble} object (output of \code{\link[=ensemble]{ensemble()}}).}

\item{level}{Numeric in (0, 1). Target two-sided coverage. Default
\code{DEFAULT_UQ_LEVEL} (0.90).}

\item{seed}{Integer or NULL. Base seed for the calibration partition
(\code{+ 1000} is applied internally). \code{NULL} (default) resolves to the seed
recorded on the ensemble contract.}

\item{verbose}{Logical. Emit the degradation note when UQ cannot be
computed. Default \code{TRUE}.}
}
\value{
\code{x} with \verb{$ensemble$uq} populated (or unchanged when calibration
is not possible). The bundle carries: \code{method} (\code{"cv_plus"}),
\code{ensemble_method}, \code{members}, \code{fold_models}, \code{calib} (\code{.row}, \code{fold},
\code{.pred_oof}, \code{truth}, \code{residual}), \code{n_calib}, \code{level_default}, \code{seed},
\code{oof_coverage}, \code{mean_width}, \code{timestamp}.
}
\description{
Populates \verb{$ensemble$uq} with a CV+ conformal bundle calibrated on the
meta-learner's honest out-of-fold residuals: a fresh 5-fold partition of
the meta frame (drawn at \verb{ensemble seed + 1000}, disjoint from the tuning
folds), per-fold refits of the finalized combiner with the fold models
retained, and the signed residuals CQR-style. Consumed by
\code{\link[=predict.horizons_ensemble]{predict.horizons_ensemble()}} when \code{interval = TRUE}.

Runs automatically inside \code{\link[=ensemble]{ensemble()}} (\code{compute_uq = TRUE}); call it
directly only to retrofit UQ onto an ensemble built with
\code{compute_uq = FALSE} or to recalibrate at a different level.

Degrades gracefully: when calibration is impossible (fewer than
\code{N_CALIB_MIN} rows, invalid order-statistic indices, or a fold refit
failure), the bundle stays \code{NULL}, a one-line note is emitted, and the
ensemble continues to predict point-only.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-single-config.R
\name{fit_single_config}
\alias{fit_single_config}
\title{Fit a Single Model Configuration (Capture Layer)}
\usage{
fit_single_config(
  config_row,
  split_F,
  cv_resamples,
  calib_data = NULL,
  train_data = NULL,
  role_map,
  best_params_eval,
  final_bayesian_iter = DEFAULT_FINAL_BAYES_ITER,
  grid_size = DEFAULT_GRID_SIZE,
  compute_uq = FALSE,
  compute_ad = FALSE,
  allow_par = FALSE,
  seed = 42L,
  sg_window = DEFAULT_SG_WINDOW,
  pca_threshold = DEFAULT_PCA_THRESHOLD,
  response_fences = NULL
)
}
\arguments{
\item{config_row}{Single-row tibble from \code{config$configs}.}

\item{split_F}{An \code{rsplit} object (Split F: train_F / test_F).}

\item{cv_resamples}{A \code{vfold_cv} object created from train_Fit (or train_F).}

\item{calib_data}{Data frame for UQ calibration (NULL if compute_uq = FALSE).}

\item{train_data}{Data frame for training. If NULL, defaults to
\code{training(split_F)}. Pass \code{train_Fit} when \code{compute_uq = TRUE} to avoid
calibration leakage.}

\item{role_map}{Tibble with \code{variable} and \code{role} columns.}

\item{best_params_eval}{Single-row tibble of best params from evaluate(),
or NULL (\code{fit()}'s cold start), in which case the re-tune starts from a
space-filling grid.}

\item{final_bayesian_iter}{Integer. Bayesian iterations for re-tuning.}

\item{grid_size}{Integer. Grid size for warm-start exploration.}

\item{compute_uq}{Logical. Whether to train UQ components.}

\item{allow_par}{Logical. Passed to tune control functions.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{sg_window}{Odd integer. Savitzky-Golay window in grid points, passed
to \code{\link[=build_recipe]{build_recipe()}}. \code{fit()} reads it from \code{configure()}'s
\code{config$recipe}, the value \code{evaluate()} ran with. Default 9.}

\item{pca_threshold}{Numeric. Variance share the \code{pca} feature selection
keeps, passed to \code{\link[=build_recipe]{build_recipe()}}. Read the same way. Default 0.995.}

\item{response_fences}{Numeric \code{c(lower = , upper = )} or \code{NULL}. The
training-partition fences \code{evaluate()} trimmed response outliers by
(#77), passed by \code{fit()} when that trim removed rows. The CV folds then
run on rows inside the fences while the test rows are untrimmed, so the
degradation check takes its test RPD over the test rows inside the
fences; the reported test metrics stay untrimmed. Default \code{NULL} (no
trim: the check reads the test RPD).}
}
\value{
List with fields: config_id, status, degraded, degraded_reason,
fitted_workflow, best_params, warm_start, start_grid_size,
cv_predictions, test_metrics, cv_metrics, uq, warnings, error_message,
runtime_secs. \code{warm_start} is \code{TRUE} when the re-tune started from
\code{best_params_eval}, \code{FALSE} when it fell back to a space-filling grid of
\code{start_grid_size} points, and \code{NA} (both) when the config failed
before tuning.
}
\description{
Inner-loop function for \code{fit()}. Takes a single config row from the
evaluate() leaderboard, re-tunes it with warm-start Bayesian optimization,
generates OOF predictions, fits a final deployable model, evaluates on
the held-out test set, and detects performance degradation.

This function is the \strong{capture layer} — it runs silently and returns a
structured list. All console output (tree rendering, progress messages) is
the responsibility of the calling \code{fit()} function.
}
\keyword{internal}

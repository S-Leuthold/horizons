% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-ensemble.R
\name{ensemble}
\alias{ensemble}
\title{Combine Fitted Models into a Stacked Ensemble}
\usage{
ensemble(
  x,
  method = "penalized",
  optimize = TRUE,
  seed = DEFAULT_ENSEMBLE_SEED,
  compute_uq = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{horizons_fit} object (output of \code{\link[=fit]{fit()}}).}

\item{method}{Character. Meta-learner to use: \code{"penalized"} (default),
\code{"weighted"}, or \code{"xgb"}.}

\item{optimize}{Logical. Tune the meta-learner's hyperparameters by CV on
the out-of-fold matrix (\code{TRUE}), or use fixed defaults (\code{FALSE}).
Default \code{TRUE}.}

\item{seed}{Integer. Random seed for the meta-learner's CV folds (used by
the \code{penalized} and \code{xgb} engines for both tuning and the genuine
out-of-fold meta predictions; recorded on the contract for all methods).
Ensemble UQ draws its calibration partition at \code{seed + 1000}, disjoint
from the tuning folds. Mirrors the \code{seed} argument of \code{\link[=fit]{fit()}} and
\code{\link[=evaluate]{evaluate()}}. Default \code{DEFAULT_ENSEMBLE_SEED} (307).}

\item{compute_uq}{Logical. Calibrate CV+ conformal prediction intervals for
the ensemble (see \code{\link[=fit_ensemble_uq]{fit_ensemble_uq()}}), stored in \verb{$ensemble$uq} and
consumed by \code{predict(..., interval = TRUE)}. Mirrors the \code{compute_uq}
argument of \code{\link[=fit]{fit()}}. Placed after \code{seed} so pre-existing positional
callers (\code{method}, \code{optimize}, \code{seed}) are unaffected. Default \code{TRUE}.}

\item{verbose}{Logical. Print the progress tree to the console. Default
\code{TRUE}.}
}
\value{
A \code{horizons_ensemble} object (inherits from \code{horizons_fit}) with the
fitted ensemble in the \code{ensemble} slot.
}
\description{
Trains a meta-learner over the models produced by \code{\link[=fit]{fit()}} and returns a
\code{horizons_ensemble} object carrying the fitted ensemble alongside the
original fit. Three meta-learners are available:
\itemize{
\item \code{"penalized"} (default): a glmnet penalized linear regression that learns
regularized member weights.
\item \code{"weighted"}: a fixed weighted average, weighting members by inverse
out-of-fold RMSE (or equally when \code{optimize = FALSE}).
\item \code{"xgb"}: an xgboost meta-learner that can capture non-linear interactions
between members.
}

All three train on the members' out-of-fold predictions and are evaluated on
the held-out Split-F test set, so the reported ensemble metrics are directly
comparable to the single-model metrics from \code{fit()}.
}
\examples{
\dontrun{
fitted <- fit(evaluated, n_best = 5)

# Default penalized meta-learner (CV+ intervals calibrated by default)
ens <- ensemble(fitted)

# Weighted average, no tuning
ens <- ensemble(fitted, method = "weighted", optimize = FALSE)

# Skip interval calibration (point predictions only)
ens <- ensemble(fitted, compute_uq = FALSE)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates-soil-evaluate_predictions.R
\name{evaluate_predictions}
\alias{evaluate_predictions}
\title{Evaluate Predicted Covariates Against Measured Values}
\usage{
evaluate_predictions(measured_data, modeled_data)
}
\arguments{
\item{measured_data}{A \code{data.frame} or \code{tibble} containing observed covariate values.
Must include a \code{Sample_ID} column and one or more numeric columns representing measured covariates.}

\item{modeled_data}{A \code{data.frame} or \code{tibble} containing predicted covariate values.
Must also include \code{Sample_ID} and matching column names for predicted covariates.}
}
\value{
A \code{tibble} summarizing evaluation metrics for each covariate. Includes columns:
\itemize{
\item \strong{Covariate}: Name of the covariate being evaluated
\item \strong{RMSE}: Root Mean Squared Error
\item \strong{R2}: Coefficient of Determination
\item \strong{CCC}: Concordance Correlation Coefficient
\item \strong{RPIQ}: Ratio of Performance to Interquartile Distance
\item Other metrics computed by \code{soilspec::eval()}
}
}
\description{
Compares predicted and observed covariate values by \code{Sample_ID}, computes evaluation metrics
for each covariate independently, and returns a tidy summary of model performance. Metrics include
RMSE, R², CCC, RPIQ, and others via \code{soilspec::eval()}. Errors or non-numeric columns are gracefully skipped.
}
\details{
The function performs an inner join on \code{Sample_ID}, appending \verb{_measured} and \verb{_modeled} suffixes
to corresponding columns. Only numeric covariates with valid observed and predicted values are evaluated.
Any covariate with missing data or evaluation errors is skipped, with a warning logged via \code{cli::cli_alert_warning()}.

Metrics are computed using \code{soilspec::eval()} with \code{obj = "quant"} and \code{na.rm = TRUE}.
This function is especially useful for assessing external validation or covariate prediction workflows.
}
\examples{
\dontrun{
results <- evaluate_predictions(
  measured_data = my_measured_data,
  modeled_data  = my_predicted_data
)
}

}
\seealso{
\code{\link[soilspec]{eval}}
}

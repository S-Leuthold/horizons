% Generated by roxygen2: do not edit by hand
% Please edit documentation in
%   R/analysis-hierarchical_interaction_decomposition.R
\name{hierarchical_interaction_decomposition}
\alias{hierarchical_interaction_decomposition}
\title{Hierarchical Interaction Decomposition Analysis}
\usage{
hierarchical_interaction_decomposition(
  results_data,
  metric = "rrmse",
  covariate_handling = c("simplified", "aggregate", "detailed"),
  max_interaction_level = 4
)
}
\arguments{
\item{results_data}{A data frame containing model evaluation results with
\code{config_desc} column and performance metric columns.}

\item{metric}{Character. Performance metric to analyze (e.g., "rrmse", "rsq", "rmse").}

\item{covariate_handling}{Character. How to handle covariate complexity:
"simplified" (group into categories), "aggregate" (Present/None), or "detailed".}

\item{max_interaction_level}{Integer. Maximum interaction level to test (2-5).
Higher levels require more data and computational time.}
}
\value{
List containing:
\itemize{
\item \code{variance_explained}: Data frame showing R² for each interaction level
\item \code{models}: List of fitted lm() objects for each level
\item \code{component_data}: Parsed component data used for analysis
\item \code{anova_results}: ANOVA results showing significance of interaction terms
}
}
\description{
Decomposes model performance into main effects, pairwise interactions,
three-way interactions, etc. to understand which interaction levels
contribute most to performance variation across component combinations.
}
\details{
\strong{Analysis Strategy:}
Fits a series of increasingly complex regression models:
\enumerate{
\item \strong{Null Model}: Intercept only (baseline)
\item \strong{Main Effects}: Each component individually
\item \strong{Pairwise Interactions}: All A×B combinations
\item \strong{Three-way Interactions}: All A×B×C combinations
\item \strong{Four-way Interactions}: All A×B×C×D combinations
\item \strong{Five-way Interaction}: A×B×C×D×E (if data allows)
}

\strong{Variance Decomposition:}
Shows how much additional variance each interaction level explains:
\itemize{
\item Main effects: 60\% (components matter individually)
\item Pairwise: +25\% (some combinations are synergistic)
\item Three-way: +10\% (complex interactions exist)
\item Higher-order: +5\% (diminishing returns)
}

\strong{Interpretation Guide:}
\itemize{
\item High main effects → Focus on individual component selection
\item High pairwise interactions → Visualize interaction matrices
\item High three-way+ interactions → Consider network approaches
}
}
\examples{
\dontrun{
# Analyze interaction structure
decomp_results <- hierarchical_interaction_decomposition(
  results_data = batch_summary,
  metric = "rrmse",
  covariate_handling = "simplified",
  max_interaction_level = 4
)

# View variance explained by each level
print(decomp_results$variance_explained)

# Plot variance decomposition
plot_interaction_decomposition(decomp_results)
}

}
\seealso{
\code{\link{plot_interaction_decomposition}}, \code{\link{extract_top_interactions}}

Other analysis: 
\code{\link{analyze_component_synergy_effects}()}
}
\concept{analysis}

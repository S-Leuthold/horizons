% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-backtransform.R
\name{get_original_scale_predictions}
\alias{get_original_scale_predictions}
\title{Get Predictions from Workflow with Automatic Back-transformation}
\usage{
get_original_scale_predictions(
  fitted_workflow,
  new_data,
  transformation,
  warn = FALSE
)
}
\arguments{
\item{fitted_workflow}{A fitted workflow object}

\item{new_data}{Data frame to predict on}

\item{transformation}{Character string indicating transformation type
("log", "sqrt", "none", etc.)}

\item{warn}{Logical. Issue warnings for edge cases in back-transformation?
Default: FALSE (suppressed for batch operations)}
}
\value{
Numeric vector of predictions on original scale
}
\description{
Extracts predictions from a fitted workflow and automatically back-transforms
them based on the transformation type. This ensures predictions are always
returned on the original scale, regardless of what transformation was used
during model training.

This helper centralizes the "predict + back-transform" pattern used across
all ensemble methods (stacks, weighted_average, xgb_meta).
}
\examples{
\dontrun{
# Get predictions already on original scale
preds <- get_original_scale_predictions(
  fitted_wf,
  test_data,
  transformation = "log"
)
}
}
\keyword{internal}

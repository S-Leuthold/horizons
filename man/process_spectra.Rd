% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_transform_spectra.R
\name{process_spectra}
\alias{process_spectra}
\title{Spectral Preprocessing Step for Recipes}
\usage{
process_spectra(input_vector, preprocessing, window_size = 9)
}
\arguments{
\item{preprocessing}{A string defining the transformation type (see details).}

\item{window_size}{Odd integer window size for Savitzky-Golay filter (default = 9).}

\item{recipe}{A \code{recipe} object. The step will be added to the sequence of operations.}

\item{...}{Selector functions to choose which variables to transform (e.g., \code{all_predictors()}).}

\item{role}{Role assigned to transformed variables (default: "predictor").}

\item{trained}{Logical. Is the step already trained? (Required by \code{recipes} API.)}

\item{skip}{Should this step be skipped when baking? (default: FALSE)}

\item{id}{Unique identifier for the step (auto-generated).}
}
\value{
A recipe step of class \code{step_transform_spectra}.
}
\description{
Applies spectral preprocessing (Savitzky-Golay, SNV, etc.) to row-wise spectral data
within a \code{recipes} pipeline. Supports trimming and transformation of each row vector using
\code{prospectr} functions.
}
\details{
Supported \code{preprocessing} values:
\itemize{
\item "No Preprocessing"
\item "Savitzky Golay - 0 Deriv"
\item "Standard Normal Variate - Savitzky Golay - 0 Deriv"
\item "Savitzky Golay - 1 Deriv"
\item "Standard Normal Variate - Savitzky Golay - 1 Deriv"
}
}
\examples{
\dontrun{
recipe(data = soil_data) \%>\%
  step_transform_spectra(all_predictors(), preprocessing = "Savitzky Golay - 0 Deriv")
}
}

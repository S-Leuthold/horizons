% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble-helpers.R
\name{build_oof_matrix}
\alias{build_oof_matrix}
\title{Build the Canonical Out-of-Fold Prediction Matrix}
\usage{
build_oof_matrix(object, members)
}
\arguments{
\item{object}{A \code{horizons_fit} object.}

\item{members}{Character vector of member \code{config_id}s (from
\code{\link[=gather_members]{gather_members()}}).}
}
\value{
A list with:
\describe{
\item{predictors}{Tibble, one row per \code{.row}, one column per member
(named \verb{member_<config_id>}), holding original-scale OOF predictions.}
\item{truth}{Numeric vector of observed values, aligned to
\code{predictors} row order.}
\item{row}{Integer vector of \code{.row} ids, aligned to \code{predictors}.}
\item{members}{The member \code{config_id}s, in column order.}
}
}
\description{
The single artifact every meta-learner engine consumes. Reshapes the flat
\code{cv_predictions} tibble (one row per sample-per-config) into a wide matrix
where each column is one member's out-of-fold prediction and each row is a
training sample, aligned by \code{.row}. The aligned \code{truth} vector travels with
it.

Predictions are taken from \code{.pred} — the original response scale. The
transformed-scale \code{.pred_trans} column is deliberately not used: the
meta-learner trains on original-scale member predictions so that members
fit under different response transformations are combined on a common
scale. (This is the cardinal back-transform invariant: \code{fit()} has already
back-transformed \code{.pred} exactly once.)
}
\keyword{internal}

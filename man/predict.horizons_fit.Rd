% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-predict.R
\name{predict.horizons_fit}
\alias{predict.horizons_fit}
\title{Predict Soil Properties from a Fitted Horizons Object}
\usage{
\method{predict}{horizons_fit}(
  object,
  new_data,
  config = "best",
  interval = TRUE,
  abstain_ood = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{horizons_fit} object (the output of \code{\link[=fit]{fit()}}).}

\item{new_data}{Spectra to predict. Either a \code{horizons_data} object or a
tibble/data.frame whose predictor (wavelength) columns match the training
schema. \strong{The spectra must already be on the training axis} — \code{predict()}
replays per-config recipe steps (preprocessing, feature selection) but
does not re-run object-level \code{standardize()}. Resample/trim/water-band
alignment is the caller's responsibility (see Details).}

\item{config}{Which fitted model to use. One of:
\itemize{
\item \code{"best"} (default): the top-ranked config by the object's rank metric.
\item a \code{config_id} string: a specific fitted model.
\item \code{"all"}: every successfully fitted model (long output, one block per
config, with a \code{config_id} column).
}}

\item{interval}{Logical. Return conformal prediction intervals when UQ is
available? Default \code{TRUE}. Ignored (with a note) for configs that have no
UQ bundle.}

\item{abstain_ood}{Logical. When \code{TRUE}, \code{.pred} (and any interval bounds)
are set to \code{NA} for samples flagged out-of-domain (\code{.ad_flag == "OOD"}),
while \code{.ad_distance}/\code{.ad_flag} are preserved so the caller can see how far
out each sample was. Default \code{FALSE} (report AD, predict for all samples).
Has no effect on configs without an AD bundle.}

\item{...}{Unused; present for S3 method consistency.}
}
\value{
A tibble in long format, one row per sample (per config when
\code{config = "all"}):
\describe{
\item{sample_id}{Sample identifier from \code{new_data}.}
\item{config_id}{Config used (present when \code{config = "all"}).}
\item{.pred}{Point prediction, original response scale.}
\item{.pred_lower}{Lower interval bound (if \code{interval = TRUE} and UQ
available), original scale.}
\item{.pred_upper}{Upper interval bound.}
\item{.interval_width}{\code{.pred_upper - .pred_lower}.}
\item{.ad_distance}{Squared Mahalanobis distance to the training centroid
in model feature space (if the config has an AD bundle).}
\item{.ad_flag}{Applicability-domain bin: \code{Q1}-\code{Q4} (within domain,
increasing distance) or \code{OOD} (out-of-domain).}
}
}
\description{
Generates predictions for new spectra using the model(s) trained by
\code{\link[=fit]{fit()}}. By default it uses the single best-ranked configuration; pass
\code{config} to select a specific model or to return predictions from every
fitted model. When uncertainty quantification was computed during \code{fit()}
(\code{compute_uq = TRUE}), prediction intervals are returned by default.
}
\details{
\strong{Axis alignment (v1 limitation).} The fitted workflow's recipe applies the
per-config preprocessing (SNV, derivatives, feature selection) but not the
object-level operations performed by \code{standardize()} (resampling to a common
wavenumber grid, range trimming, water-band removal). \code{new_data} must
therefore arrive on the same wavenumber axis the model was trained on.
\code{predict()} validates the predictor schema and errors if it does not match.

Applicability-domain columns (\code{.ad_distance}, \code{.ad_flag}) are emitted per
config when the object carries an AD bundle (\code{fit(compute_ad = TRUE)}). The
distance is a squared Mahalanobis distance in the model's feature space; the
flag bins it against held-out-calibrated thresholds. Set \code{abstain_ood = TRUE}
to \code{NA} predictions for out-of-domain samples while keeping the AD columns.

\strong{Response upper bound (guardrail).} Point predictions are winsorized to
\code{models$response_bound} (max training outcome times 1.5, stored by \code{\link[=fit]{fit()}})
with a visible warning when any value is clamped. This catches physically
impossible back-transform blow-ups (e.g. an unconstrained log-scale
prediction inflating through \code{exp()}) while permitting modest extrapolation.
Interval bounds are deliberately NOT clamped — truncating the interval would
overstate confidence exactly where the model is least trustworthy. Objects
fitted before this field existed predict without a clamp.
}
\examples{
\dontrun{
fitted <- fit(evaluated, n_best = 5, compute_uq = TRUE)

# Best model, with intervals
predict(fitted, new_spectra)

# A specific config, point predictions only
predict(fitted, new_spectra, config = "cubist_log_snv_cars", interval = FALSE)

# Every fitted model
predict(fitted, new_spectra, config = "all")
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble-xgb.R
\name{fit_ensemble_xgb}
\alias{fit_ensemble_xgb}
\title{Fit an XGBoost Meta-Learner Ensemble}
\usage{
fit_ensemble_xgb(
  object,
  members,
  oof,
  rank_metric,
  optimize = TRUE,
  seed = DEFAULT_ENSEMBLE_SEED
)
}
\arguments{
\item{object}{A \code{horizons_fit} object.}

\item{members}{Character vector of member \code{config_id}s.}

\item{oof}{The out-of-fold matrix list from \code{\link[=build_oof_matrix]{build_oof_matrix()}}.}

\item{rank_metric}{Character. Metric the object was ranked by.}

\item{optimize}{Logical. Tune the xgboost hyperparameters (\code{TRUE}) or use
conservative fixed defaults (\code{FALSE}). Default \code{TRUE}.}

\item{seed}{Integer. Seed for the CV folds (passed through to
\code{\link[=fit_tuned_meta_learner]{fit_tuned_meta_learner()}} so the meta-OOF is reproducible).}
}
\value{
The ensemble contract list from \code{\link[=build_ensemble_contract]{build_ensemble_contract()}}.
}
\description{
Trains an xgboost model as the meta-learner over member out-of-fold
predictions. With \code{optimize = TRUE} (default), \code{trees}, \code{tree_depth}, and
\code{learn_rate} are tuned by CV on the OOF matrix; with \code{optimize = FALSE},
conservative defaults for a small meta-feature space are used
(\code{trees = 500}, \code{tree_depth = 3}, \code{learn_rate = 0.05}).

The meta-model's relative weighting of members is reported as feature
importance (xgboost has no coefficients); the \code{model} slot holds the fitted
workflow used to combine new predictions.
}
\keyword{internal}

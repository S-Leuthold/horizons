% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visuals-plot_models_radar.R
\name{plot_models_radar}
\alias{plot_models_radar}
\title{Visualize Marginal Effects of Model Pipeline Components}
\usage{
plot_models_radar(
  results_data,
  metric = "rrmse",
  components = c("ModelType", "Transformation", "Preprocessing", "Feature_Selection",
    "Covariates"),
  plot_type = c("radar", "bar"),
  normalize = TRUE,
  title = NULL,
  color_scheme = c("default", "viridis", "plasma"),
  min_variation = 0.01,
  group_by = NULL
)
}
\arguments{
\item{results_data}{A data frame containing model evaluation results with at minimum:
a \code{config_desc} column with workflow identifiers and performance metric columns.}

\item{metric}{Character. Performance metric to analyze (e.g., "rrmse", "rsq", "rmse").
Must be a column name in \code{results_data}.}

\item{components}{Character vector. Pipeline components to analyze. Default includes
all standard components: \code{c("ModelType", "Transformation", "Preprocessing",  "Feature_Selection", "Covariates")}.}

\item{plot_type}{Character. Type of visualization: \code{"radar"} (default) for radar/spider
plots, or \code{"bar"} for horizontal bar charts.}

\item{normalize}{Logical. If \code{TRUE} (default), normalize marginal standard deviations
to 0-100 scale for easier comparison across metrics.}

\item{title}{Character. Optional plot title. If \code{NULL}, generates automatic title.}

\item{color_scheme}{Character. Color palette: \code{"default"} (package colors),
\code{"viridis"}, or \code{"plasma"}.}

\item{min_variation}{Numeric. Minimum coefficient of variation required for a component
to be included in analysis. Default: 0.01 (1\%).}

\item{group_by}{Character. Optional column name for creating faceted plots by group
(e.g., different response variables or datasets).}
}
\value{
A \code{ggplot2} object. For radar plots, returns a polar coordinate plot.
For bar plots, returns a horizontal bar chart. Can be further customized with
standard \code{ggplot2} functions.
}
\description{
Creates radar or bar plots showing the marginal contribution of different
modeling components (model type, preprocessing, transformations, feature selection,
covariates) to prediction performance variability. Useful for identifying which
pipeline components have the greatest impact on model performance.
}
\details{
This function analyzes the marginal contribution of each pipeline component by:
\enumerate{
\item Parsing config descriptors to extract component values
\item Computing marginal standard deviation for each component while holding others constant
\item Creating visualizations showing relative importance
}

\strong{Marginal Analysis Method:}
For each component, the function groups results by all other components and calculates
the standard deviation of the performance metric within each group. The average of
these standard deviations represents the marginal contribution of that component.

\strong{Config Descriptor Format:}
Expects workflow identifiers in the format: \code{model_transformation_preprocessing_featureselection_covariates}
(e.g., \code{"cubist_Log_SNVD1_PCA_Clay-pH-CEC"}). The function uses robust parsing to handle
variations in format.

\strong{Error Handling:}
Uses the package's \code{safely_execute()} framework for robust error handling.
Provides meaningful error messages and graceful degradation for partial failures.
}
\examples{
\dontrun{
# Basic radar plot
plot_models_radar(
  results_data = model_results,
  metric = "rrmse"
)

# Bar chart with custom components
plot_models_radar(
  results_data = model_results,
  metric = "rsq",
  components = c("ModelType", "Preprocessing", "Covariates"),
  plot_type = "bar",
  title = "Component Effects on R-squared"
)

# Faceted plot by response variable
plot_models_radar(
  results_data = multi_response_results,
  metric = "rrmse",
  group_by = "Response_Variable",
  normalize = TRUE
)
}

}
\seealso{
\code{\link{plot_ensemble_upset}}, \code{\link{plot_ensemble_biplot}},
\code{\link{run_model_evaluation}}

Other visualization: 
\code{\link{plot_component_chord_diagram}()},
\code{\link{plot_component_effects_enhanced}()},
\code{\link{plot_component_interactions}()},
\code{\link{plot_component_network_bundling}()},
\code{\link{plot_component_value_interactions}()}
}
\concept{visualization}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble-weighted.R
\name{fit_ensemble_weighted}
\alias{fit_ensemble_weighted}
\title{Fit a Weighted-Average Ensemble}
\usage{
fit_ensemble_weighted(object, members, oof, rank_metric, optimize = TRUE)
}
\arguments{
\item{object}{A \code{horizons_fit} object.}

\item{members}{Character vector of member \code{config_id}s (from
\code{\link[=gather_members]{gather_members()}}).}

\item{oof}{The out-of-fold matrix list from \code{\link[=build_oof_matrix]{build_oof_matrix()}}.}

\item{rank_metric}{Character. Metric the object was ranked by (for the
improvement comparison).}

\item{optimize}{Logical. Inverse-RMSE weighting (\code{TRUE}) or equal weights
(\code{FALSE}). Default \code{TRUE}.}
}
\value{
The ensemble contract list from \code{\link[=build_ensemble_contract]{build_ensemble_contract()}}.
}
\description{
Combines member predictions by a fixed weighted average. With
\code{optimize = TRUE} (default) weights are proportional to inverse out-of-fold
RMSE, so more accurate members contribute more; with \code{optimize = FALSE}
every member is weighted equally (a plain average — the honest no-tuning
baseline).

The weights come from the out-of-fold matrix, but the reported \code{test_F}
predictions come from each member predicting the held-out set fresh (via
\code{predict_one_config()}, which predicts once and back-transforms once) and
then being combined. This keeps the evaluation honest: weights are learned
on OOF data, performance is measured on data no member saw.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation-local-single.R
\name{evaluate_single_model_local}
\alias{evaluate_single_model_local}
\title{Evaluate a Single Model Configuration (Local Execution)}
\usage{
evaluate_single_model_local(
  config_row,
  input_data,
  data_split,
  config_id,
  covariate_data = NULL,
  variable,
  output_dir = NULL,
  grid_size = 10,
  bayesian_iter = 15,
  cv_folds = 5,
  parallel_cv = TRUE,
  n_cv_cores = NULL,
  prune_models = FALSE,
  prune_threshold = 0.9,
  seed = 123
)
}
\arguments{
\item{config_row}{Single row from configuration tibble}

\item{input_data}{Full dataset (for recipe building)}

\item{data_split}{rsample split object (for consistent train/test splits)}

\item{config_id}{Unique identifier for progress tracking}

\item{covariate_data}{Optional tibble with covariates (full dataset)}

\item{variable}{Response variable name}

\item{output_dir}{Directory for saving outputs}

\item{grid_size}{Grid search size}

\item{bayesian_iter}{Bayesian optimization iterations}

\item{cv_folds}{Number of CV folds}

\item{parallel_cv}{Logical. Use parallel processing for CV}

\item{n_cv_cores}{Number of cores for parallel CV (NULL = auto-detect)}

\item{prune_models}{Logical. Skip Bayesian optimization for poor models (default FALSE)}

\item{prune_threshold}{Numeric. Multiplier for baseline RRMSE (default 0.9 = must beat baseline by 10\%)}

\item{seed}{Random seed for reproducibility}
}
\value{
Tibble row with model configuration and performance metrics
}
\description{
Core function for evaluating a single model configuration in local execution mode.
Accepts pre-split data to ensure fair model comparison across all configurations.
Uses NULL/non-NULL covariates pattern (no include_covariates flag needed).
}
\keyword{internal}

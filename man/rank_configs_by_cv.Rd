% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-evaluate.R
\name{rank_configs_by_cv}
\alias{rank_configs_by_cv}
\title{Rank evaluation result rows on the cross-validated metric}
\usage{
rank_configs_by_cv(results, metric)
}
\arguments{
\item{results}{Tibble of evaluation result rows (\code{evaluation$results}
shape).}

\item{metric}{Bare metric name, e.g. \code{"rpd"}. Direction comes from
\code{HIGHER_BETTER_METRICS}.}
}
\value{
\code{results} with unrankable rows removed, ordered best-first.
}
\description{
Orders result rows best-first by \verb{cv_<metric>}, the cross-validated mean at
each config's selected hyperparameters. This is the single ranking rule for
\code{evaluate()}'s \code{best_config} and \code{fit()}'s member selection, so the two can
never disagree, and neither touches a test-set column (#50).

Rows whose \verb{cv_<metric>} is \code{NA} (checkpoints written before the column
existed, or a config whose CV panel could not be recovered) are dropped
with a warning naming them. If no row can be ranked, aborts with the
remedy: re-run \code{evaluate()}.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step-transform-spectra.R
\name{transform_spectra_matrix}
\alias{transform_spectra_matrix}
\title{Transform a Spectral Matrix}
\usage{
transform_spectra_matrix(X, preprocessing, window_size = 9)
}
\arguments{
\item{X}{Numeric matrix (or something \code{as.matrix()} turns into one). One
spectrum per row.}

\item{preprocessing}{Character. One of the methods documented in
\code{\link[=step_transform_spectra]{step_transform_spectra()}}.}

\item{window_size}{Odd integer. Savitzky-Golay window size. Default 9.}
}
\value{
A numeric matrix with \code{nrow(X)} rows and
\code{ncol(X) - (window_size - 1)} columns, without dimnames. Rows whose input
is entirely non-finite come back entirely \code{NA}; no error is raised for
them, so callers that need to surface such rows must check.
}
\description{
Applies one of the supported preprocessing methods to a whole spectral
matrix (rows are spectra, columns are wavenumbers in monotonic order) in a
single prospectr call. This is the implementation behind
\code{bake.step_transform_spectra()}, and the unit a caller can use to apply the
same preprocessing outside a recipe.

Every method is row-wise, so the result for a given spectrum does not
depend on which other rows are present. Output width is
\code{ncol(X) - (window_size - 1)} for every method: \code{raw} and \code{snv} drop
\code{(window_size - 1) / 2} columns from each edge explicitly, the
Savitzky-Golay methods lose the same edge inside \code{prospectr::savitzkyGolay()}.
}
\keyword{internal}

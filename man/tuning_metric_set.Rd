% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{tuning_metric_set}
\alias{tuning_metric_set}
\title{Build the Metric Set Used During Hyperparameter Tuning}
\usage{
tuning_metric_set(
  transformation,
  metrics = c("rmse", "rrmse", "rsq", "mae", "rpd", "ccc")
)
}
\arguments{
\item{transformation}{Character scalar: \code{"none"}, \code{"log"}, \code{"log10"} or
\code{"sqrt"}. The same vocabulary as \code{\link[=back_transform_predictions]{back_transform_predictions()}}. Unknown
values abort rather than silently scoring cross-scale.}

\item{metrics}{Character vector naming the metrics to include, in order.
Any of \code{"rmse"}, \code{"rrmse"}, \code{"rsq"}, \code{"mae"}, \code{"rpd"}, \code{"ccc"}.}
}
\value{
A \code{yardstick} metric set.
}
\description{
Returns the \code{yardstick::metric_set()} that \code{evaluate_single_config()} and
\code{fit_single_config()} hand to \code{tune::tune_grid()} and \code{tune::tune_bayes()}.
For \code{transformation = "none"} it is the plain set of the requested metrics.
For \code{"log"}, \code{"log10"} and \code{"sqrt"} every metric is wrapped so that the
estimate is back-transformed with \code{\link[=back_transform_predictions]{back_transform_predictions()}} before it
is compared to the truth, and the whole set is therefore scored on the
original response scale.
}
\details{
\strong{Why this exists.} \code{build_recipe()} applies the response transformation
as a recipe step with \code{skip = TRUE}, which is the correct setting for a
transformation that must not run at predict time. But \code{skip = TRUE} also
means the step never runs when tune bakes an assessment set, so the
\code{truth} column tune scores against stays on the original scale while the
model's predictions are on the transformed scale. Without this wrapper every
tuning metric is a cross-scale comparison: \code{select_best()}, the
\code{tune_bayes()} acquisition target, and the prune gate all optimise a number
dominated by the scale offset rather than by model quality (issue #49).

Scoring on the original scale, rather than forward-transforming \code{truth} to
score on the transformed scale, is a deliberate choice: the leaderboard, the
prune threshold and the reported test metrics are all original-scale, so
hyperparameters are selected on the same quantity that is reported. It also
means selection sees retransformation bias, which a transformed-scale score
never would.

The structural alternative, transforming the outcome column before the
recipe so tune sees one scale by construction, is the tidymodels-canonical
shape and is deferred until after the JOSS submission because it changes
every reader of \code{collect_predictions()}, including the conformal residuals.
This factory compensates for the seam; it does not remove it.

Metric names and directions are preserved exactly (\code{.metric == "rmse"}
stays \code{"rmse"}, \code{rpd} stays \code{"maximize"}), so callers that select or rank
by name need no change. The first metric in \code{metrics} is the one
\code{tune_bayes()} optimises; keep \code{"rmse"} first where that matters.
}
\seealso{
\code{\link[=back_transform_predictions]{back_transform_predictions()}}, \code{\link[=compute_original_scale_metrics]{compute_original_scale_metrics()}}
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/library-targets.R
\name{apply_bounds}
\alias{apply_bounds}
\title{Apply Bounds to Predictions}
\usage{
apply_bounds(values, property, verbose = TRUE)
}
\arguments{
\item{values}{Numeric vector. Predicted values to bound.}

\item{property}{Character. Property name (determines which bounds to apply).}

\item{verbose}{Logical. If TRUE (default), warns when clipping occurs.}
}
\value{
Numeric vector with bounds enforced. Same length as input.
}
\description{
Enforces property-specific constraints on predictions including pH bounds
and non-negativity requirements. Ensures predictions are physically valid.
}
\details{
Different properties have different constraints:

\strong{pH}: Bounded to [0, 14]
\itemize{
\item Theoretical range of pH scale
\item Values clipped to this range
}

\strong{Carbon/Nitrogen/Elements}: Non-negative
\itemize{
\item Negative concentrations are physically impossible
\item Values clipped to ≥ 0
}

\strong{Other properties}: No bounds applied
\itemize{
\item CEC, base cations, etc. can be high (no upper bound)
}
}
\section{Properties with Constraints}{

\itemize{
\item \strong{pH}: [0, 14]
\item \strong{oc, soc, total_carbon, carbonate}: ≥ 0
\item \strong{total_nitrogen}: ≥ 0
\item \strong{iron_total, aluminum_total}: ≥ 0
}
}

\examples{
\dontrun{
# Clip pH to valid range
ph_bounded <- apply_bounds(c(-1, 5, 8, 15), "ph")
# Result: c(0, 5, 8, 14)

# Enforce non-negativity for carbon
oc_bounded <- apply_bounds(c(-5, 0, 10, 50), "oc")
# Result: c(0, 0, 10, 50)
}

}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-fit.R
\name{calib_split_seed}
\alias{calib_split_seed}
\title{Seed for fit()'s calibration split}
\usage{
calib_split_seed(seed)
}
\arguments{
\item{seed}{Integer seed passed to \code{fit()}.}
}
\value{
Integer seed for Split C.
}
\description{
\code{fit()} scores on \code{evaluate()}'s train/test split and carves the
calibration set that UQ and AD share (Split C) out of its training part.
Split C is seeded on its own, with \code{seed + 1L}, so the calibration rows
depend only on \code{seed} and those training rows, not on RNG state left by an
earlier draw. The offset is documented rather than hidden so a caller who
needs to reproduce the partition can.
}
\keyword{internal}

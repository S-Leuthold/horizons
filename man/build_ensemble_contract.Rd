% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble-helpers.R
\name{build_ensemble_contract}
\alias{build_ensemble_contract}
\title{Assemble the Unified Ensemble Contract}
\usage{
build_ensemble_contract(
  method,
  model,
  weights,
  ensemble_pred,
  member_pred,
  rank_metric,
  runtime_secs,
  oof_pred = NULL
)
}
\arguments{
\item{method}{Character. The meta-learner method (\code{"penalized"},
\code{"weighted"}, \code{"xgb"}).}

\item{model}{The fitted meta-learner object (engine-specific).}

\item{weights}{Tibble with one row per member: \code{member} (config_id) and
\code{coef} (the normalized contribution — stacks coefficient, inverse-RMSE
weight, or xgb importance, depending on method).}

\item{ensemble_pred}{Tibble of ensemble \code{test_F} predictions: \code{sample_id},
\code{.pred}, \code{truth} (original scale).}

\item{member_pred}{Tibble of per-member \code{test_F} predictions: \code{config_id},
\code{sample_id}, \code{.pred}, \code{truth} (original scale). Used for \code{member_metrics}
and the improvement baseline.}

\item{rank_metric}{Character. The metric the object was ranked by; the
axis on which improvement is measured.}

\item{runtime_secs}{Numeric. Engine wall-clock, for the record.}

\item{oof_pred}{Tibble or NULL. The ensemble's combined out-of-fold
predictions (\code{.row}, \code{.pred}, \code{truth}, original scale) — the by-product
each engine already computes to derive its weights. Carried for Phase-2
ensemble UQ (stacked conformal calibrates on these residuals); unused in
Phase 1. Default NULL.}
}
\value{
A list matching the \code{ensemble} slot contract: \code{method}, \code{model},
\code{weights}, \code{predictions}, \code{metrics}, \code{member_metrics}, \code{improvement},
\code{oof_predictions}, \code{uq} (NULL in Phase 1), \code{timestamp}, \code{runtime_secs}.
}
\description{
Packs an engine's outputs into the canonical \code{ensemble} slot that every
meta-learner method populates identically. This is pure assembly plus
centralized scoring — it fits nothing. Engines fit their combiner and hand
back the fitted model, the member weights, and \code{test_F} predictions; all
performance scoring happens here so that metrics, member metrics, and the
improvement comparison are computed the same way for every method.

Scoring reuses \code{\link[=compute_original_scale_metrics]{compute_original_scale_metrics()}} — the same six-metric
computer the single-model path uses — so an ensemble's metrics are directly
comparable to its members' metrics.
}
\keyword{internal}

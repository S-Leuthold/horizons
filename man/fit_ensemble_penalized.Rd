% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble-penalized.R
\name{fit_ensemble_penalized}
\alias{fit_ensemble_penalized}
\title{Fit a Penalized-Regression Ensemble}
\usage{
fit_ensemble_penalized(
  object,
  members,
  oof,
  rank_metric,
  optimize = TRUE,
  seed = DEFAULT_ENSEMBLE_SEED
)
}
\arguments{
\item{object}{A \code{horizons_fit} object.}

\item{members}{Character vector of member \code{config_id}s.}

\item{oof}{The out-of-fold matrix list from \code{\link[=build_oof_matrix]{build_oof_matrix()}}.}

\item{rank_metric}{Character. Metric the object was ranked by.}

\item{optimize}{Logical. Tune penalty/mixture (\code{TRUE}) or use fixed
defaults (\code{FALSE}). Default \code{TRUE}.}

\item{seed}{Integer. Seed for the CV folds (passed through to
\code{\link[=fit_tuned_meta_learner]{fit_tuned_meta_learner()}} so the meta-OOF is reproducible).}
}
\value{
The ensemble contract list from \code{\link[=build_ensemble_contract]{build_ensemble_contract()}}.
}
\description{
Trains a glmnet penalized linear regression as the meta-learner over member
out-of-fold predictions. With \code{optimize = TRUE} (default), \code{penalty} and
\code{mixture} are tuned by k-fold CV on the OOF matrix; with \code{optimize = FALSE},
fixed defaults are used (\code{penalty = 0.01}, \code{mixture = 1}).

Combination of new predictions runs through the fitted meta-model, so the
\code{model} slot (the fitted workflow) and the \code{weights} slot (the glmnet
coefficients, for interpretation) are distinct.
}
\keyword{internal}

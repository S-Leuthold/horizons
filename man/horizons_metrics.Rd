% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{horizons_metrics}
\alias{horizons_metrics}
\alias{rpd_vec}
\alias{rpd}
\alias{rrmse_vec}
\alias{rrmse}
\alias{ccc_vec}
\alias{ccc}
\title{Custom yardstick metrics for spectroscopic modeling}
\usage{
rpd_vec(truth, estimate, na_rm = TRUE, ...)

rpd(data, truth, estimate, na_rm = TRUE, ...)

rrmse_vec(truth, estimate, na_rm = TRUE, ...)

rrmse(data, truth, estimate, na_rm = TRUE, ...)

ccc_vec(truth, estimate, na_rm = TRUE, ...)

ccc(data, truth, estimate, na_rm = TRUE, ...)
}
\arguments{
\item{truth}{Numeric vector of observed values.}

\item{estimate}{Numeric vector of predicted values.}

\item{na_rm}{Logical. Remove NAs before computation? Default TRUE.}

\item{...}{Not used.}

\item{data}{A data frame with truth and estimate columns.}
}
\value{
The \verb{*_vec()} functions return a single numeric value. The metric objects
(\code{rpd}, \code{rrmse}, \code{ccc}) and their data-frame methods return a one-row
tibble in the standard \code{yardstick} format (\code{.metric}, \code{.estimator},
\code{.estimate}).
}
\description{
Regression metrics commonly used to evaluate spectroscopic calibrations,
implemented as \code{yardstick} metrics so they compose with the rest of the
tidymodels evaluation stack. Each metric provides the standard three-layer
interface: a vectorized \verb{*_vec()} function, a data-frame method, and a
\code{yardstick} metric object.
\itemize{
\item \strong{RPD} (ratio of performance to deviation): the standard deviation of
the observed values divided by the RMSE of prediction. Higher is better.
\item \strong{RRMSE} (relative root mean squared error): the RMSE expressed relative
to the mean of the observed values. Lower is better.
\item \strong{CCC} (Lin's concordance correlation coefficient): agreement between
observed and predicted values, combining precision and accuracy. Ranges
from -1 to 1, with 1 indicating perfect concordance.
}
}

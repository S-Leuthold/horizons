% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-ensemble-stacks.R
\name{apply_stacks_weights}
\alias{apply_stacks_weights}
\title{Apply Stacks Weights to Base Predictions}
\usage{
apply_stacks_weights(base_predictions, coefficients)
}
\arguments{
\item{base_predictions}{Data frame of base model predictions. One column
per model, all on the same scale (original scale after back-transformation).}

\item{coefficients}{Named numeric vector of weights. Names may be numeric
indices ("1", "2", "3") from stacks or actual member names.}
}
\value{
Numeric vector of ensemble predictions, one per row in \code{base_predictions}.
}
\description{
Applies ensemble weights (coefficients) to base model predictions to
generate the final ensemble prediction. This performs a weighted sum
of the base predictions using the coefficients determined by the
LASSO meta-learner.
}
\examples{
\dontrun{
base_preds <- tibble(model_1 = c(5, 10), model_2 = c(6, 11))
coefs <- c(model_1 = 0.6, model_2 = 0.4)
ensemble_pred <- apply_stacks_weights(base_preds, coefs)
# Returns: c(5.4, 10.4)  # 5*0.6 + 6*0.4 = 5.4
}
}
\keyword{internal}

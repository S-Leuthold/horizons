% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate-single-config.R
\name{evaluate_single_config}
\alias{evaluate_single_config}
\title{Evaluate a Single Model Configuration}
\usage{
evaluate_single_config(
  config_row,
  split,
  cv_folds,
  role_map,
  grid_size = DEFAULT_GRID_SIZE,
  bayesian_iter = DEFAULT_BAYES_ITER,
  prune = FALSE,
  prune_threshold = 1,
  allow_par = FALSE,
  parallel_over = "resamples",
  seed = 42L,
  sg_window = DEFAULT_SG_WINDOW,
  pca_threshold = DEFAULT_PCA_THRESHOLD,
  outcome_range = DEFAULT_OUTCOME_RANGE
)
}
\arguments{
\item{config_row}{Single-row tibble from \code{config$configs}. Must contain:
\code{config_id}, \code{model}, \code{transformation}, \code{preprocessing},
\code{feature_selection}, \code{covariates}.}

\item{split}{An \code{rsplit} object from \code{rsample::initial_split()}.}

\item{cv_folds}{A \code{vfold_cv} object created from the training set.}

\item{role_map}{Tibble with \code{variable} and \code{role} columns.}

\item{grid_size}{Integer. Number of grid points for \code{tune_grid()}.}

\item{bayesian_iter}{Integer. Iterations for \code{tune_bayes()}. Set to 0 to
skip Bayesian optimization entirely.}

\item{prune}{Logical. If TRUE, record whether the best grid-search RPD
falls below \code{prune_threshold} (\code{below_prune_threshold}), and when it
does, skip Bayesian optimization and label the config \code{"pruned"}. With
\code{bayesian_iter = 0} there is nothing to skip, so the config is labelled
\code{"success"} and only \code{below_prune_threshold} carries the reading.}

\item{prune_threshold}{Numeric. RPD threshold for pruning, on the original
response scale (tuning metrics are scored there via
\code{tuning_metric_set()}). Only used when \code{prune = TRUE}. Default 1.0, as
in \code{evaluate()}.}

\item{allow_par}{Logical. Passed to \code{tune::control_grid()} and
\code{tune::control_bayes()} to enable parallel CV folds on the registered
\code{future::plan()}. \code{evaluate()} sets this from the resolved axis: \code{FALSE}
on the configs axis (tune ships nothing inward), \code{TRUE} on the
resamples axis.}

\item{parallel_over}{Character. tune's \code{parallel_over}, passed through to
the control objects. \code{"resamples"} (default) ships one rsplit per task;
\code{"everything"} ships the whole rset to every task. tune may rewrite the
value before dispatch (parameter-free configs become \code{"resamples"};
single-split rsets become \code{"everything"}).}

\item{seed}{Integer. Random seed for reproducibility.}

\item{sg_window}{Odd integer. Savitzky-Golay window in grid points, passed
to \code{\link[=build_recipe]{build_recipe()}}. \code{evaluate()} reads it from \code{configure()}'s
\code{config$recipe}. Default 9.}

\item{pca_threshold}{Numeric. Variance share the \code{pca} feature selection
keeps, passed to \code{\link[=build_recipe]{build_recipe()}}. \code{evaluate()} reads it from
\code{configure()}'s \code{config$recipe}. Default 0.995.}

\item{outcome_range}{Numeric length-2 vector. The outcome's physical range,
which the back-transformed test-set predictions (and, for a transformed
response, the tuning predictions) are clamped to before scoring.
\code{evaluate()} reads it from \code{configure()}'s \code{config$outcome_range}.
Default \code{c(0, Inf)}.}
}
\value{
Single-row tibble with columns: \code{config_id}, \code{status} (\code{"pruned"}
means Bayesian refinement was skipped), \code{below_prune_threshold} (logical;
\code{NA} when \code{prune = FALSE}), \code{prune_threshold} (the threshold that reading
was taken against; \code{NA} when \code{prune = FALSE}), the six
test-set metrics \code{rmse}, \code{rrmse}, \code{rsq}, \code{ccc}, \code{rpd}, \code{mae}, the six
cross-validated means at the selected hyperparameters \code{cv_rmse},
\code{cv_rrmse}, \code{cv_rsq}, \code{cv_ccc}, \code{cv_rpd}, \code{cv_mae} (what \code{evaluate()} and
\code{fit()} rank on), \code{best_params} (list-column), \code{error_message},
\code{warnings}, \code{runtime_secs}.
}
\description{
Inner-loop function for \code{evaluate()}. Takes a single config row, builds a
recipe + model workflow, tunes hyperparameters via grid search (optionally
followed by Bayesian optimization), evaluates on the held-out test set, and
returns a single-row result tibble.

This function is the \strong{capture layer} — it runs silently and returns
structured data. All console output (tree rendering, progress messages) is
the responsibility of the calling \code{evaluate()} function.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-select-training.R
\name{select_training}
\alias{select_training}
\title{Pipeline: Select a Training Set From a Pool}
\usage{
select_training(
  x,
  pool,
  k = 400L,
  scope = c("batch", "cluster", "sample", "global"),
  properties = NULL,
  snv = TRUE,
  derivative = 1L,
  window = 11L,
  poly = 2L,
  mask = NULL,
  space = c("pca", "pls"),
  ncomp = 0.99,
  metric = c("mahalanobis", "euclidean", "cosine"),
  clusters = NULL,
  cluster_min = 30L,
  twin_ratio = SELECT_TWIN_RATIO,
  chunk_size = 500L,
  seed = 1L,
  verbose = TRUE
)
}
\arguments{
\item{x}{\code{horizons_data.} The targets: the samples to be predicted.}

\item{pool}{\code{horizons_data.} The reference pool, with one or more
response columns.}

\item{k}{\code{integer.} Neighbours per target per property; a scalar, or a
named vector with one entry per property. Default: \code{400}.}

\item{scope}{\code{character.} \code{"batch"}, \code{"cluster"}, \code{"sample"} or
\code{"global"}. Default: \code{"batch"}.}

\item{properties}{\verb{character or NULL.} Response columns of the pool to
draw for. \code{NULL} means all of them. Default: \code{NULL}.}

\item{snv}{\code{logical.} SNV in the similarity space. Default: \code{TRUE}.}

\item{derivative}{\code{integer.} Savitzky-Golay derivative order in the
similarity space; \code{0} disables the filter. Default: \code{1}.}

\item{window, poly}{\code{integer.} Savitzky-Golay window and polynomial
order. Default: \code{11}, \code{2}.}

\item{mask}{\verb{matrix or NULL.} Wavenumber ranges to drop from the
similarity space after the derivative, one row per range, low then
high. Default: \code{NULL}.}

\item{space}{\code{character.} \code{"pca"} (unsupervised, of the pool) or
\code{"pls"} (against one property; \code{properties} must name exactly one).
Default: \code{"pca"}.}

\item{ncomp}{\code{numeric.} Components: a proportion of variance in (0, 1)
(PCA only, capped at 100) or an integer count. Default: \code{0.99}.}

\item{metric}{\code{character.} \code{"mahalanobis"}, \code{"euclidean"} or
\code{"cosine"}, on the scores. Default: \code{"mahalanobis"}.}

\item{clusters}{\verb{integer or NULL.} \code{scope = "cluster"} only: the cluster
count, or \code{NULL} to choose by silhouette. Default: \code{NULL}.}

\item{cluster_min}{\code{integer.} \code{scope = "cluster"} only: the floor on
cluster size. Default: \code{30}.}

\item{twin_ratio}{\code{numeric.} The nearest-to-second-nearest ratio below
which a pool row is a target's twin. Default: an internal constant to
be calibrated on replicate scans.}

\item{chunk_size}{\code{integer.} Targets per distance chunk. Default: \code{500}.}

\item{seed}{\code{integer.} Seed for the clustering. Default: \code{1}.}

\item{verbose}{\code{logical.} Print the report. Default: \code{TRUE}.}
}
\value{
\code{horizons_data.} Rows drawn from \code{pool}, on the targets'
wavenumber grid, with every response column the pool carried, three
provenance columns with role \code{meta} (\code{.drawn_by}, \code{.min_distance},
and \code{.group} except under \code{scope = "sample"}), and the record in
\code{x$selection}.
}
\description{
Draws a training set from a reference pool (a spectral library with
measured properties) around a batch of target spectra, and returns it as
an ordinary \code{horizons_data}. Everything downstream, \code{configure()},
\code{validate()}, \code{evaluate()}, \code{fit()}, \code{predict()}, runs unchanged; the
model never learns it was trained on a selection.
}
\details{
\strong{The rule.} For each target and each requested property, the \code{k}
nearest pool rows that have that property measured. Rows enter the
training set once, however many targets drew them, and are not
weighted. Drawing per property keeps \code{k} usable rows per target for
every property, so a sparse property is reported honestly rather than
silently thin.

\strong{Scopes.} All four draw the same per-target neighbourhoods; they differ
in how those are grouped into training sets, and the grouping lives in
the record (\code{x$selection$groups}), not in the shape of the return:
\itemize{
\item \code{"batch"} (default): one training set, the union over every target.
\item \code{"cluster"}: the targets are clustered in the similarity space
(k-means, count by silhouette unless \code{clusters} fixes it, clusters
under \code{cluster_min} merged into the nearest); one group per cluster.
\item \code{"sample"}: one group per target, its own \code{k} nearest.
\item \code{"global"}: no draw; the whole pool, kept so the comparison is one
argument away.
}

For \code{"cluster"} and \code{"sample"} the return is still the single union, and
fitting one model per group is the caller's loop over
\code{x$selection$groups} for now.

\strong{The similarity space.} Distances are measured in a space built for
"similar soil", not "similar baseline", and it never reaches the model:
a user who configures \code{preprocessing = "raw"} still gets neighbours
chosen on derivative spectra. The default chain is SNV, a Savitzky-Golay
first derivative (window 11, order 2), PCA of the pool to 99 \% of
variance, and Mahalanobis distance on the scores. Every step is an
argument.

\strong{Reconciliation.} The targets' wavenumbers are the grid. The pool is
resampled onto them through the same routine \code{standardize()} uses, so
the training set and the targets share one axis end to end. A pool that
does not cover the targets' range stops the verb; targets finer than the
pool warn.

\strong{Self-leakage.} A pool row whose spectrum is a target's twin (nearest
distance zero, or below \code{twin_ratio} times the second-nearest) is
excluded from that target's neighbourhood and reported.

\strong{What the record holds} (\code{x$selection}): the settings as resolved, the
reconciliation, the pool's identity, the membership table (target by
property by pool row, with distance and rank), the groups, pool sizes
per property against those available, every target's nearest and
mean-of-k distance (the applicability signal at selection time), the
twins excluded, and the clustering when \code{scope = "cluster"}.
}
\examples{
\dontrun{
training <- targets |>
  select_training(pool, k = 400, properties = "clay")

model <- training |>
  configure(outcome = "clay") |>
  validate() |>
  evaluate() |>
  fit()

predict(model, targets)
}

}

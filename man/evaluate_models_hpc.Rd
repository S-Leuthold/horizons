% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation-hpc.R
\name{evaluate_models_hpc}
\alias{evaluate_models_hpc}
\title{HPC Evaluation Orchestrator}
\usage{
evaluate_models_hpc(
  config,
  input_data,
  covariate_data = NULL,
  variable,
  output_dir = NULL,
  grid_size = 10,
  bayesian_iter = 15,
  cv_folds = 10,
  outer_workers = NULL,
  inner_workers = NULL,
  seed = 307,
  prune_models = FALSE,
  prune_threshold = 0.8,
  resume = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{config}{Data frame of model configurations}

\item{input_data}{Data frame with spectral data}

\item{covariate_data}{Optional data frame with covariates}

\item{variable}{Character. Name of response variable}

\item{output_dir}{Character. Directory for results}

\item{grid_size}{Integer. Grid search size (default: 10)}

\item{bayesian_iter}{Integer. Bayesian optimization iterations (default: 15)}

\item{cv_folds}{Integer. Cross-validation folds (default: 10)}

\item{outer_workers}{Integer. Number of concurrent models (auto if NULL)}

\item{inner_workers}{Integer. Workers per model (auto if NULL)}

\item{seed}{Integer. Random seed (default: 307)}

\item{resume}{Logical. Resume from checkpoints (default: TRUE)}

\item{verbose}{Logical. Print progress (default: TRUE)}
}
\value{
List with evaluation results
}
\description{
Runs model evaluation using nested parallelization for HPC environments.
Respects resources allocated by the scheduler (SLURM/PBS) or uses available cores.
}

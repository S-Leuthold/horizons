% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates-clustering.R
\name{create_clustered_subsets}
\alias{create_clustered_subsets}
\title{Create Clustered Training Subsets}
\usage{
create_clustered_subsets(
  ossl_pca_scores,
  cluster_model,
  prop = 0.85,
  verbose = TRUE
)
}
\arguments{
\item{ossl_pca_scores}{Tibble with all OSSL PCA scores + covariate columns
(Dim.1, Dim.2, ..., clay, ph, oc, etc.)}

\item{cluster_model}{List from cluster_ossl_samples() with cluster_assignments}

\item{prop}{Numeric. Training proportion (default: 0.85)}

\item{verbose}{Logical. Print progress (default: TRUE)}
}
\value{
Named list of lists, one per cluster:
Cluster_1 = list(train = tibble(...), val = tibble(...))
Cluster_2 = list(train = tibble(...), val = tibble(...))
...
}
\description{
Partitions OSSL training data into cluster-specific subsets with train/val
splits. Each cluster gets its own training and validation sets, preserving
all covariate columns.
}
\keyword{internal}

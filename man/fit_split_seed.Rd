% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-fit.R
\name{fit_split_seed}
\alias{fit_split_seed}
\title{Seed for fit()'s train/test partition}
\usage{
fit_split_seed(seed)
}
\arguments{
\item{seed}{Integer seed passed to \code{fit()}.}
}
\value{
Integer seed for Split F.
}
\description{
\code{evaluate()} seeds its train/test split with \code{seed}. \code{fit()} builds its own
split with the same \code{rsample::initial_split()} call on the same frame, so
seeding it with \code{seed} too reproduced \code{evaluate()}'s partition exactly
(#50). Split F is seeded with \code{seed + 1L} instead. The offset is documented
rather than hidden so a caller who needs to reproduce the partition can.
}
\keyword{internal}

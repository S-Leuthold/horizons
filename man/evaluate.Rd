% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-evaluate.R
\name{evaluate}
\alias{evaluate}
\title{Evaluate Model Configurations}
\usage{
evaluate(
  x,
  metric = "rpd",
  prune = TRUE,
  prune_threshold = 1,
  allow_par = FALSE,
  parallelize_over = "auto",
  output_dir = NULL,
  seed = 307L,
  verbose = TRUE,
  workers = NULL
)
}
\arguments{
\item{x}{A \code{horizons_data} object with \code{config$configs} populated by
\code{configure()}.}

\item{metric}{Character. Metric for ranking configurations. One of
\code{"rpd"}, \code{"rsq"}, \code{"rmse"}, \code{"rrmse"}, \code{"ccc"}, \code{"mae"}. Ranking uses
the cross-validated value of that metric at each config's selected
hyperparameters (\verb{evaluation$results$cv_<metric>}); the test-set columns
are reported but never used for selection, so they remain honest
held-out estimates. Default \code{"rpd"}.}

\item{prune}{Logical. If TRUE, skip Bayesian optimization for configs
whose grid-search RPD falls below \code{prune_threshold}. When \code{configure()}
set \code{bayesian_iter = 0} there is nothing to skip, so no config is
pruned. Either way, whether each config fell below the threshold is
recorded in \code{evaluation$results$below_prune_threshold} (with the
threshold in \code{prune_threshold}), and \code{fit()} warns when every member it
fits did. Default TRUE.}

\item{prune_threshold}{Numeric. RPD threshold for pruning. Configs with
grid-search RPD below this value skip Bayesian optimization but still
receive test-set metrics from grid-search best. Default 1.0 (the
"no better than the mean" line).}

\item{allow_par}{Logical. If \code{TRUE}, dispatch onto whatever
\code{future::plan()} the caller has registered. If no usable backend is
registered (fewer than two workers), warns naming the plan it found and
runs sequentially. \code{FALSE} (the default) runs in the calling process and
never inspects the plan. \code{evaluate()} never registers or alters a plan.}

\item{parallelize_over}{Character. Which axis of the work the registered
plan is applied to; ignored when \code{allow_par = FALSE}. See the
Parallelism section. Default \code{"auto"}.}

\item{output_dir}{Character or NULL. If provided, each config's result is
written to \verb{<output_dir>/checkpoints/<config_id>.rds} as it finishes, and
a rerun into the same directory resumes from those files. Required when
configs are dispatched to workers. A resumed row must have been scored on
this run's training data (the outcome, the sample ids, which columns hold
the \code{id}, \code{outcome}, \code{predictor} and \code{covariate} roles, and the outcome,
predictor and covariate values) and tuned with this run's settings
(\code{cv_folds}, \code{grid_size}, \code{bayesian_iter}, \code{prune}, \code{prune_threshold}
when pruning, \code{seed}, and \code{configure()}'s \code{sg_window}, \code{pca_threshold}
and \code{outcome_range}); either mismatch aborts, naming what differs.
So re-standardized spectra, a rescaled outcome, or another outcome on the
same samples is refused rather than resumed, while adding a sibling
response with \code{add_response()} or a \code{meta} column resumes. Keep one
\code{output_dir} per outcome. The ranking \code{metric} is not checked, since
every row carries all six cross-validated metrics. Rows scored under an
earlier scoring schema are dropped and re-evaluated; rows for configs no
longer in the grid are dropped; a file that cannot be used warns and its
config is re-evaluated; rows written before a fingerprint field existed
warn once and resume. A whole-table \code{eval_checkpoint.rds} from earlier
versions is read only for configs with no per-config file, and its rows
are copied into \verb{checkpoints/}, after which it can be deleted. Default
NULL (no checkpointing).}

\item{seed}{Integer. Random seed for train/test split and CV folds.
Default 307L.}

\item{verbose}{Logical. Print progress tree to console. Default TRUE.}

\item{workers}{Deprecated (2026-09-14) and ignored, with a warning. The
earlier design took a core count and auto-split it into outer and inner
levels while managing its own nested plan. Register a plan and use
\code{allow_par} with \code{parallelize_over} instead.}
}
\value{
A \code{horizons_eval} object (inherits from \code{horizons_data}) with
\code{evaluation$results}, \code{evaluation$best_config}, \code{evaluation$split}, and
associated metadata populated, including \code{evaluation$parallelize_over}
(the axis actually used), \code{evaluation$workers} (the worker count the
registered plan offered; 1 when sequential) and \code{evaluation$recipe} (the
\code{sg_window}, its width in cm-1 on the evaluated axis as \code{sg_window_cm},
and the \code{pca_threshold} every config's recipe ran with). Test-set
predictions are clamped to \code{configure()}'s \code{outcome_range} before they
are scored, as are the tuning predictions of a transformed response,
which are back-transformed first. Aborts, before any config runs, when
\code{configure()}'s \code{sg_window} is not narrower than the spectrum, and, with class
\code{horizons_input_error}, when an observed outcome lies outside
\code{outcome_range} (#76). Called on an object that
has already been through \code{fit()} or \code{ensemble()}, it returns a
\code{horizons_eval} whose \code{models} and \code{ensemble} slots are empty again,
since both were built on the evaluation it replaces.
}
\description{
Evaluates all model configurations from \code{configure()} against held-out test
data. Each configuration is tuned via grid search (optionally followed by
Bayesian optimization), evaluated on the test set, and ranked by the
specified metric. The best configuration is stored for downstream use by
\code{fit()}.
}
\section{Parallelism}{

The user owns the backend and the topology; \code{evaluate()} owns only which
axis the plan is applied to.
\itemize{
\item \code{"configs"}: one future per config on the registered plan, with tune
running sequentially inside each. The per-worker payload is one config's
worth and tune ships nothing to inner workers. The mode for a large
config grid on a many-core machine.
\item \code{"resamples"}: configs run sequentially; inside each, tune parallelises
the CV folds on the registered plan. Useful width is capped at
\code{cv_folds}. The mode for a few cores and a short grid.
\item \code{"auto"} (default): \code{"configs"} when the number of configs is at least
\code{cv_folds}, otherwise \code{"resamples"}. Derived from the measured cost
model: configs wins whenever \code{n_configs} exceeds the effective inner
speedup, which is bounded by \code{cv_folds}, and always once it exceeds the
worker count, so the worker count cancels.
}

A nested plan (configs across an outer level, folds across an inner one)
is not built or endorsed by the package in this version. A caller who
wants one registers it with \code{future::plan(list(...))} and uses
\code{"configs"}.

\if{html}{\out{<div class="sourceCode">}}\preformatted{# Laptop: four cores, parallelise the folds inside each config.
future::plan(future::multisession, workers = 4)
hd |> evaluate(allow_par = TRUE, parallelize_over = "resamples")

# Thirty cores, one config per worker, checkpointed.
future::plan(future::multisession, workers = 30)
hd |> evaluate(allow_par = TRUE, output_dir = "output/run")
}\if{html}{\out{</div>}}

Before dispatch, BLAS, OpenMP, data.table and ranger threads are pinned to
one in the calling process (via \code{RhpcBLASctl} when installed) and restored
on exit. Configs are dispatched to a top-level worker that loads the
\emph{installed} package, so parallel dispatch refuses to run under
\code{devtools::load_all()}.
}

\section{When every configuration fails}{

\code{best_config} is chosen from the configs that succeeded or, when none did,
from the pruned configs that carry a cross-validated value of \code{metric}.
When there are neither, or none of them has a value of \code{metric} to rank
on, \code{evaluate()} aborts with class \code{horizons_all_configs_failed}. The
message lists the distinct error messages (the first three, each with the
configs that raised it) and names any configs that were loaded from
checkpoints in \code{output_dir} rather than run, since calling \code{evaluate()}
again resumes them instead of re-running them. The per-config results,
\code{error_message} included, travel on the condition as \code{results}, because
\code{evaluate()} aborts before it assigns \code{x$evaluation}; after an uncaught
abort, \code{rlang::last_error()$results} recovers them without re-running. A
loop over subsets can catch the class, tolerate one subset whose every
config fails, and keep its errors:

\if{html}{\out{<div class="sourceCode">}}\preformatted{runs <- lapply(subsets, function(hd) \{
  tryCatch(
    evaluate(hd, verbose = FALSE),
    horizons_all_configs_failed = function(e) e$results
  )
\})

# A horizons_eval for each subset that ranked, and the failed configs'
# results table for each subset that did not.
failed <- runs[!vapply(runs, inherits, logical(1), "horizons_eval")]
}\if{html}{\out{</div>}}
}


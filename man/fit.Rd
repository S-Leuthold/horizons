% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline-fit.R
\name{fit}
\alias{fit}
\title{Fit Top Model Configurations}
\usage{
fit(
  x,
  n_best = 5L,
  metric = NULL,
  compute_uq = TRUE,
  compute_ad = TRUE,
  allow_par = FALSE,
  seed = 307L,
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{horizons_eval} object (output of \code{evaluate()}), or a
configured \code{horizons_data} with exactly one configuration, which \code{fit()}
starts cold (see "Cold start from one configuration").}

\item{n_best}{Integer. Number of top configurations to re-tune. Default 5.
A cold start has one.}

\item{metric}{Character or NULL. Metric for ranking the candidate
configs, by bare name: one of \code{"rpd"}, \code{"rsq"}, \code{"rmse"}, \code{"rrmse"},
\code{"ccc"}, \code{"mae"}, or \code{fit()} aborts with class \code{horizons_input_error}.
If NULL, uses the rank_metric from \code{evaluate()}; on a cold start, where
there is nothing to rank and the metric is only recorded, the one a
cold-started fit recorded, else \code{"rpd"}. Ranking reads the
cross-validated value at each config's selected hyperparameters
(\verb{evaluation$results$cv_<metric>}), never the test-set column, so the
test set stays held out from selection. Default NULL.}

\item{compute_uq}{Logical. Train UQ components (quantile model +
conformal calibration). Default TRUE.}

\item{compute_ad}{Logical. Compute applicability-domain metadata (centroid +
shrinkage covariance + held-out OOD thresholds) per config, so \code{predict()}
can emit \code{.ad_distance} / \code{.ad_flag}. Shares the UQ calibration split.
Default TRUE.}

\item{allow_par}{Logical. If \code{TRUE}, tune parallelises the CV folds
inside each member's re-tune and OOF pass on whatever \code{future::plan()}
the caller has registered; \code{fit()} never registers a plan. If the plan
offers fewer than two workers, warns naming it and runs sequentially.
\code{fit()} parallelises folds only in this version; a configs axis across
the \code{n_best} members is gated on the fitted-object memory contract (a
fitted object is hundreds of MB and would cross to workers). Default
FALSE.}

\item{seed}{Integer. Random seed for the CV folds and, through
\code{calib_split_seed()} (\code{seed + 1L}), for the calibration split. The
train/test split is \code{evaluate()}'s, so it does not depend on this seed,
except on a cold start, where \code{fit()} draws that split at this seed as
\code{evaluate()} would. Default 307L.}

\item{verbose}{Logical. Print progress tree to console. Default TRUE.}
}
\value{
A \code{horizons_fit} object (inherits from \code{horizons_eval},
\code{horizons_data}) with \verb{models$} slot populated. The slot includes
\code{response_bound}, the deploy-time winsorization guardrail \code{predict()}
applies to back-transformed point predictions:
\code{max + (RESPONSE_BOUND_MARGIN - 1) * (max - anchor)}, capped at a finite
upper bound of \code{outcome_range}, where \code{max} is the largest training
outcome and the anchor is the lower bound of \code{outcome_range}, or the
smallest training outcome when that bound is \code{-Inf}. Under the default
range the anchor is 0 and the bound is the largest training outcome
times \code{RESPONSE_BOUND_MARGIN}, as before the range existed; for any
range it lies above the largest training outcome unless the cap binds.
The training rows are the ones the final models are fit on:
\code{evaluate()}'s training part, less the calibration set when UQ or AD is
on. The slot also includes \code{selection_present} (whether the
training object carried a \verb{$selection} from \code{select_training()}, which
\code{predict()} reads when asked for conformal intervals). Called on a
\code{horizons_ensemble}, it returns a \code{horizons_fit} whose \code{ensemble} slot
is empty again, since the ensemble was built on the members it replaces.
\code{models$results} records how each member's re-tune started:
\code{warm_start} (\code{TRUE} from \code{evaluate()}'s parameters, \code{FALSE} from a
space-filling grid) and \code{start_grid_size}.
}
\description{
Re-tunes the top N configurations from \code{evaluate()} with warm-start
Bayesian optimization, generates out-of-fold (OOF) CV predictions for
downstream stacking, fits final deployable models, scores them on
\code{evaluate()}'s held-out test rows, and optionally trains uncertainty
quantification (UQ) components.

This is the render layer — it owns all console output. The capture layer
(\code{fit_single_config()}) runs silently and returns structured results.

The train/test partition (Split F) is \code{evaluate()}'s split, reused. Its
test rows are the only rows nothing was selected on: \code{evaluate()}'s
training rows chose the members (on \verb{cv_<metric>}) and tuned the
warm-start parameters, so a partition drawn afresh would score the final
models partly on those rows. \code{fit()} checks that the split still indexes
the rows this object models: after dropping the rows whose outcome is \code{NA}
(the rule \code{evaluate()} applies), the id and outcome columns must match the
split's, value for value and in order, or \code{fit()} aborts with class
\code{horizons_input_error}. Columns added since \code{evaluate()}, such as a sibling
response from \code{add_response()}, are carried into the fit. The count of
NA-outcome rows is reported in the console tree when \code{verbose = TRUE}.
With UQ or AD on, the calibration set is carved out of Split F's training
part.

The members are the top \code{n_best} of the configurations \code{evaluate()} chose
\code{best_config} from, ranked by the same rule: the ones that succeeded or,
when none did, the pruned ones that carry a cross-validated value of
\code{metric}. When every member fell below \code{evaluate()}'s \code{prune_threshold}
(\code{evaluation$results$below_prune_threshold}), \code{fit()} warns with class
\code{horizons_below_threshold_warning}, naming the threshold and the members'
cross-validated RPD; that includes \code{bayesian_iter = 0}, where nothing is
pruned. When the members are pruned configurations the warning also
carries class \code{horizons_pruned_fallback_warning}. If every member fails,
\code{fit()} aborts with class \code{horizons_all_members_failed}, listing the
distinct error messages; the
per-member results travel on the condition as \code{results}, and
\code{models$results} keeps each member's \code{error_message} when some succeed.
}
\section{Cold start from one configuration}{

With one configuration there is nothing for \code{evaluate()} to choose, so a
configured \code{horizons_data} whose \code{config$configs} has exactly one row can
go straight to \code{fit()} (#45). \code{fit()} then draws the train/test split
itself, by the draw \code{evaluate()} uses, so at the same \code{seed} it holds out
exactly the rows \code{evaluate()} would have. With no parameters from
\code{evaluate()} to warm-start from, the re-tune starts from a space-filling
grid of \code{grid_size} points; the console tree says so, and
\code{models$results$warm_start} is \code{FALSE}. The degradation check needs
nothing from \code{evaluate()}, since it compares the test RPD with \code{fit()}'s
own cross-validation.

The fit carries an unscreened evaluation record in place of
\code{evaluate()}'s: \code{evaluation$screened} is \code{FALSE}, the one configuration is
\code{best_config} with status \code{"not_evaluated"}, its metric and \verb{cv_*} columns
are \code{NA}, and \code{runtime_secs} is 0. \code{evaluation$recipe} records the recipe
settings as \code{evaluate()} does, and \code{fit()} applies \code{evaluate()}'s checks
first: at least twice \code{cv_folds} rows with an observed outcome, and a
Savitzky-Golay window narrower than the spectrum. Re-fitting a
cold-started fit starts cold again. With more than one configuration
\code{fit()} aborts with class \code{horizons_input_error}: choosing among them is
what \code{evaluate()} is for.
}

\section{Outcome range}{

Every prediction \code{fit()} scores (out-of-fold, test and calibration) is
clamped to \code{configure()}'s \code{outcome_range}, as \code{predict()} clamps what it
serves. Before anything is drawn or fitted, on either path, \code{fit()} aborts
with class \code{horizons_input_error} when an observed outcome lies outside
the range, naming \code{outcome_range} and how to set it (#76).
}


% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/library-train.R
\name{calculate_composite_score}
\alias{calculate_composite_score}
\title{Calculate Composite Score for Config Ranking}
\usage{
calculate_composite_score(metrics, tie_threshold = 0.01)
}
\arguments{
\item{metrics}{Tibble with columns: config_id, rpd, ccc, rsq, rmse}

\item{tie_threshold}{Numeric. Score difference considered a tie. Default: 0.01}
}
\value{
Tibble with added columns:
\describe{
\item{composite_score}{Weighted normalized score (0-1, higher = better)}
\item{rank}{Integer rank (1 = best)}
}
}
\description{
Computes weighted composite score from multiple performance metrics for ranking
model configurations. Uses spectroscopy-appropriate weighting with RPD as primary.
}
\details{
\strong{Formula:}

\if{html}{\out{<div class="sourceCode">}}\preformatted{composite = 0.35 * RPD_norm +
            0.25 * CCC_norm +
            0.25 * R²_norm +
            0.15 * (1 - RMSE_norm)
}\if{html}{\out{</div>}}

\strong{Normalization:} Each metric scaled to [0, 1] within the tested configs:

\if{html}{\out{<div class="sourceCode">}}\preformatted{metric_norm = (metric - min(metric)) / (max(metric) - min(metric))
}\if{html}{\out{</div>}}

\strong{Tie-Breaking:} If scores within threshold (default 0.01), prefer:
\itemize{
\item Simpler model (fewer features)
\item Faster model (lower training time)
}
}
\keyword{internal}

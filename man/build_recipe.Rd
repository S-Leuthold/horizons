% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-recipes.R
\name{build_recipe}
\alias{build_recipe}
\title{Build a Tidymodels Recipe from a Config Row}
\usage{
build_recipe(
  config_row,
  train_data,
  role_map,
  sg_window = DEFAULT_SG_WINDOW,
  pca_threshold = DEFAULT_PCA_THRESHOLD
)
}
\arguments{
\item{config_row}{Single-row tibble from \code{config$configs}.}

\item{train_data}{Data frame. Training split containing all columns.}

\item{role_map}{Tibble with \code{variable} and \code{role} columns from the
horizons_data object.}

\item{sg_window}{Odd integer. Savitzky-Golay window, in grid points, passed
to \code{step_transform_spectra(window_size = )}. Default 9.}

\item{pca_threshold}{Numeric in (0, 1]. Share of variance the \code{pca} feature
selection keeps, passed to \code{recipes::step_pca(threshold = )}. Default
0.995.}
}
\value{
A \code{recipes::recipe} object ready for \code{workflows::add_recipe()}.
}
\description{
Translates a single config row into a tidymodels recipe. Uses \code{role_map}
to identify columns by role — no hard-coded wavenumber ranges. This is
the recipe constructor for \code{evaluate_single_config()}.

Recipe step order (early fusion):
\enumerate{
\item Response transformation (step_log/step_sqrt with skip = TRUE)
\item Spectral preprocessing (step_transform_spectra on predictor_cols only)
\item Feature selection (pca/boruta/cars/correlation/none), on spectral
columns only
\item Covariate inclusion (promote requested covariates to predictor; the rest
stay in a non-predictor hold role)
}

Columns that are neither the outcome nor spectral predictors are given an
explicit non-predictor role — \code{id}, \code{meta}, \code{response_hold} (sibling lab
responses) and \code{covariate_hold}. Nothing is left to the \code{outcome ~ .}
default, which would make a predictor of any column the role map did not
name.

A final pass re-points the step selector quosures away from this function's
frame, which would otherwise be serialized to every parallel worker. See
\code{strip_selector_envs()}.

\code{sg_window} and \code{pca_threshold} are object-level: \code{configure()} records one
value of each and every config's recipe uses it, so neither is part of the
config id. The window is in grid points, and it trims \code{(sg_window - 1) / 2}
columns from each end of the spectrum for every preprocessing method, \code{raw}
and \code{snv} included, so the output is \code{p - (sg_window - 1)} columns wide. The
Savitzky-Golay polynomial order is not a setting: each method fixes its own
(\code{sg} p = 1; \code{deriv1} m = 1, p = 1; \code{deriv2} m = 2, p = 3; the \code{snv_}
variants the same). \code{pca_threshold} is read only by
\code{feature_selection = "pca"}. \code{configure()} validates both; this function
passes them through as given.
}
\keyword{internal}

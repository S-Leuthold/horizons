% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble-helpers.R
\name{fit_tuned_meta_learner}
\alias{fit_tuned_meta_learner}
\title{Fit a Tuned Meta-Learner Over the Out-of-Fold Matrix}
\usage{
fit_tuned_meta_learner(
  object,
  members,
  oof,
  rank_metric,
  optimize,
  method,
  spec,
  grid,
  extract_weights,
  seed = DEFAULT_ENSEMBLE_SEED
)
}
\arguments{
\item{object}{A \code{horizons_fit} object.}

\item{members}{Character vector of member \code{config_id}s.}

\item{oof}{The out-of-fold matrix list from \code{\link[=build_oof_matrix]{build_oof_matrix()}}.}

\item{rank_metric}{Character. Metric to tune and rank on.}

\item{optimize}{Logical. Tune hyperparameters (\code{TRUE}) or fit the spec as-is
(\code{FALSE}, spec carries fixed values).}

\item{method}{Character. Method label for the contract and error messages.}

\item{spec}{A \code{parsnip} model spec (with \code{tune()} placeholders when
\code{optimize = TRUE}, fixed values otherwise).}

\item{grid}{The tuning grid (ignored when \code{optimize = FALSE}).}

\item{extract_weights}{A function \verb{(meta_fit, members) -> tibble(member, coef)} that reads the member weighting from the fitted model.}

\item{seed}{Integer. Seed set immediately before the CV folds are drawn, so
the tuning resamples and the genuine meta-OOF are reproducible.}
}
\value{
The ensemble contract list from \code{\link[=build_ensemble_contract]{build_ensemble_contract()}}.
}
\description{
The shared scaffold behind the \code{penalized} and \code{xgb} engines: build the
meta-training frame from the OOF matrix, optionally tune the meta-learner's
hyperparameters by CV on that matrix (the tuning resamples come only from the
OOF matrix — \code{test_F} never enters the tuning path, so the reported
performance is honest), refit the winner on the full matrix, then have the
members predict \code{test_F} and combine those predictions \emph{through} the fitted
meta-model. The engine supplies the model spec, the tuning grid, and a
closure that extracts member weights from the fitted model.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_run_bayes_tuning.R
\name{run_bayesian_tuning}
\alias{run_bayesian_tuning}
\title{Run Bayesian Tuning on Filtered Workflows}
\usage{
run_bayesian_tuning(tuned_wf_set, folds, parallel = FALSE, iterations = 15)
}
\arguments{
\item{tuned_wf_set}{A filtered \code{workflow_set} returned by \code{\link[=filter_workflows]{filter_workflows()}},
with prior tuning results stored in the \code{result} column.}

\item{folds}{An object created by \code{\link[rsample:vfold_cv]{rsample::vfold_cv()}} or similar,
specifying the resampling strategy for Bayesian tuning.}

\item{parallel}{Logical. If TRUE (default), tuning is parallelized
across models using \code{future::multisession}. Otherwise runs sequentially.}

\item{iterations}{Integer. Number of Bayesian optimization iterations
per model (default = 15).}
}
\value{
A \code{workflow_set} object with updated \code{result} and \code{metrics} columns,
containing the results of Bayesian tuning.
}
\description{
Applies Bayesian hyperparameter optimization using \code{tune::tune_bayes()} to a
filtered \code{workflow_set}. The function reconstructs workflows from previous
tuning results and optionally runs in parallel using \code{furrr::future_map2()}.
Each model is tuned using the initial grid search results as priors.
}
\details{
This function assumes all preprocessing functions used in workflows
(e.g., \code{step_transform_spectra}) have been sourced into the current session.
Each model is tuned independently using \code{\link[tune:tune_bayes]{tune::tune_bayes()}}, with the
initial search informed by results from \code{\link[tune:tune_grid]{tune::tune_grid()}}.
}
\examples{
\dontrun{
folds <- rsample::vfold_cv(training_data, v = 5)
bayes_tuned <- run_bayesian_tuning(tuned_wf_set = filtered_workflows,
                                   folds        = folds,
                                   parallel     = TRUE,
                                   iterations   = 15)
}

}
\seealso{
\code{\link[tune:tune_bayes]{tune::tune_bayes()}}, \code{\link[workflowsets:workflow_set]{workflowsets::workflow_set()}}, \code{\link[=filter_workflows]{filter_workflows()}},
\code{\link[yardstick:metric_set]{yardstick::metric_set()}}
}
\keyword{internal}
